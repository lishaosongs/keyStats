<Window x:Class="KeyStats.Views.StatsPopupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:KeyStats.Views.Controls"
        xmlns:viewmodels="clr-namespace:KeyStats.ViewModels"
        xmlns:p="clr-namespace:KeyStats.Properties"
        Title="KeyStats"
        Width="360"
        SizeToContent="Height"
        WindowStyle="None"
        AllowsTransparency="False"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        ResizeMode="NoResize"
        Deactivated="Window_Deactivated"
        Opacity="0">
    <Window.RenderTransform>
        <TranslateTransform x:Name="WindowTransform"/>
    </Window.RenderTransform>

    <Window.DataContext>
        <viewmodels:StatsPopupViewModel/>
    </Window.DataContext>

    <Border x:Name="RootBorder"
            Background="{DynamicResource SurfaceBrush}"
            BorderThickness="0"
            CornerRadius="8">
        <ScrollViewer VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      CanContentScroll="False">
            <StackPanel Margin="10">
            <!-- Header -->
            <Grid Margin="2,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{x:Static p:Strings.Stats_TodayHeader}" FontSize="14" FontWeight="SemiBold"
                           Foreground="{DynamicResource TextPrimaryBrush}"/>
                <ToggleButton x:Name="PeakToggle" Grid.Column="2"
                              IsChecked="{Binding IsPeakPopupOpen, Mode=TwoWay}"
                              Background="Transparent" BorderThickness="0"
                              Cursor="Hand" Padding="4,2" VerticalAlignment="Center">
                    <ToggleButton.Template>
                        <ControlTemplate TargetType="ToggleButton">
                            <Border x:Name="Bd" Background="Transparent" CornerRadius="4" Padding="{TemplateBinding Padding}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE945;" FontFamily="Segoe MDL2 Assets" FontSize="12"
                                               Foreground="{DynamicResource AccentBrush}" VerticalAlignment="Center"/>
                                    <StackPanel Margin="3,0,0,0">
                                        <TextBlock Text="{Binding DataContext.PeakKPS, RelativeSource={RelativeSource AncestorType=Window}}"
                                                   FontSize="9" LineHeight="12" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                        <TextBlock Text="{Binding DataContext.PeakCPS, RelativeSource={RelativeSource AncestorType=Window}}"
                                                   FontSize="9" LineHeight="12" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SubtleFillBrush}"/>
                                </Trigger>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SubtleFillBrush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </ToggleButton.Template>
                </ToggleButton>
                <Popup Grid.Column="2"
                       IsOpen="{Binding IsPeakPopupOpen, Mode=TwoWay}"
                       StaysOpen="False"
                       AllowsTransparency="True"
                       PopupAnimation="Fade"
                       Placement="Bottom"
                       PlacementTarget="{Binding ElementName=PeakToggle}"
                       HorizontalOffset="-40">
                    <Border Background="{DynamicResource SurfaceBrush}"
                            BorderBrush="{DynamicResource DividerBrush}"
                            BorderThickness="1" CornerRadius="8"
                            Padding="14,10" Margin="8">
                        <StackPanel>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="16"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" MinWidth="70">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                        <TextBlock Text="&#xE945;" FontFamily="Segoe MDL2 Assets" FontSize="10"
                                                   Foreground="{DynamicResource AccentBrush}" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                        <TextBlock Text="{x:Static p:Strings.Stats_PeakKpsTooltipLabel}" FontSize="10"
                                                   Foreground="{DynamicResource TextSecondaryBrush}"/>
                                    </StackPanel>
                                    <TextBlock Text="{Binding PeakKPS}" FontSize="20" FontWeight="SemiBold"
                                               Foreground="{DynamicResource TextPrimaryBrush}"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2" MinWidth="70">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                        <TextBlock Text="&#xE945;" FontFamily="Segoe MDL2 Assets" FontSize="10"
                                                   Foreground="{DynamicResource AccentBrush}" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                        <TextBlock Text="{x:Static p:Strings.Stats_PeakCpsTooltipLabel}" FontSize="10"
                                                   Foreground="{DynamicResource TextSecondaryBrush}"/>
                                    </StackPanel>
                                    <TextBlock Text="{Binding PeakCPS}" FontSize="20" FontWeight="SemiBold"
                                               Foreground="{DynamicResource TextPrimaryBrush}"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>
                </Popup>
            </Grid>

            <!-- Stats Cards -->
            <Grid Margin="0,0,0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="4"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Style="{DynamicResource CardBorder}" Padding="9,8">
                    <controls:StatItemControl Icon="&#xE765;" Title="{x:Static p:Strings.Stats_KeyPresses}" Value="{Binding KeyPresses}"/>
                </Border>
                <Border Grid.Column="2" Style="{DynamicResource CardBorder}" Padding="9,8">
                    <controls:StatItemControl Icon="&#xE8B0;" Title="{x:Static p:Strings.Stats_MouseClicks}" Value="{Binding TotalClicks}"/>
                </Border>
            </Grid>

            <Border Style="{DynamicResource CardBorder}" Margin="0,0,0,4" Padding="10,9">
                <StackPanel>
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0"
                                Background="{DynamicResource SubtleFillBrush}"
                                CornerRadius="5"
                                Width="24"
                                Height="24"
                                Margin="0,0,8,0"
                                VerticalAlignment="Center">
                            <TextBlock Text="&#xE8B0;"
                                       FontFamily="Segoe MDL2 Assets"
                                       FontSize="12"
                                       Foreground="{DynamicResource AccentBrush}"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Grid.Column="1"
                                   Text="{x:Static p:Strings.Stats_MouseClickDetail}"
                                   FontSize="10"
                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                   VerticalAlignment="Center"/>
                    </Grid>

                    <Grid Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="6"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="6"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Background="{DynamicResource SubtleFillBrush}" CornerRadius="7" Padding="10,8">
                            <StackPanel>
                                <TextBlock Text="{x:Static p:Strings.Click_Left}" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                <TextBlock Text="{Binding LeftClicks}" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="2" Background="{DynamicResource SubtleFillBrush}" CornerRadius="7" Padding="10,8">
                            <StackPanel>
                                <TextBlock Text="{x:Static p:Strings.Click_Middle}" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                <TextBlock Text="{Binding MiddleClicks}" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="4" Background="{DynamicResource SubtleFillBrush}" CornerRadius="7" Padding="10,8">
                            <StackPanel>
                                <TextBlock Text="{x:Static p:Strings.Click_Right}" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                <TextBlock Text="{Binding RightClicks}" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <Grid>
                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsSideClickRowVisible}" Value="False">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="6"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Background="{DynamicResource SubtleFillBrush}" CornerRadius="7" Padding="10,8">
                            <StackPanel>
                                <TextBlock Text="{x:Static p:Strings.Click_Back}" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                <TextBlock Text="{Binding SideBackClicks}" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="2" Background="{DynamicResource SubtleFillBrush}" CornerRadius="7" Padding="10,8">
                            <StackPanel>
                                <TextBlock Text="{x:Static p:Strings.Click_Forward}" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                <TextBlock Text="{Binding SideForwardClicks}" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </StackPanel>
            </Border>

            <Grid Margin="0,0,0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="4"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Style="{DynamicResource CardBorder}" Padding="8,7">
                    <controls:StatItemControl Icon="&#xE8AB;" Title="{x:Static p:Strings.Stats_MouseDistance}" Value="{Binding MouseDistance}"/>
                </Border>
                <Border Grid.Column="2" Style="{DynamicResource CardBorder}" Padding="8,7">
                    <controls:StatItemControl Icon="&#xE74A;" Title="{x:Static p:Strings.Stats_ScrollDistance}" Value="{Binding ScrollDistance}"/>
                </Border>
            </Grid>

            <!-- Key Breakdown -->
            <Grid Margin="4,8,0,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="6"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{x:Static p:Strings.Stats_KeyBreakdown}" Style="{DynamicResource SectionTitle}" Margin="0" VerticalAlignment="Center"/>
                <Button Grid.Column="1" Content="{x:Static p:Strings.Stats_KeyboardHeatmap}" Style="{DynamicResource GhostButton}"
                        Click="OpenKeyboardHeatmap_Click" Padding="12,5"/>
                <Button Grid.Column="3" Content="{x:Static p:Strings.Stats_KeyHistory}" Style="{DynamicResource GhostButton}"
                        Click="OpenKeyHistory_Click" Padding="12,5"/>
            </Grid>
            <Border Style="{DynamicResource CardBorder}" Padding="8,6" Margin="0,0,0,6">
                <controls:KeyBreakdownControl
                    Column1Items="{Binding Column1Items}"
                    Column2Items="{Binding Column2Items}"
                    Column3Items="{Binding Column3Items}"/>
            </Border>

            <!-- App Breakdown -->
            <Grid Margin="4,8,0,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{x:Static p:Strings.Stats_ActiveApps}" Style="{DynamicResource SectionTitle}" Margin="0" VerticalAlignment="Center"/>
                <Button Grid.Column="1" Content="{x:Static p:Strings.Stats_AppStatsDetail}" Style="{DynamicResource GhostButton}"
                        Click="OpenAppStats_Click" Padding="12,5"/>
            </Grid>
            <Border Style="{DynamicResource CardBorder}" Padding="8,6" Margin="0,0,0,6">
                <ListBox ItemsSource="{Binding AppStatsItems}"
                         MaxHeight="100"
                         Background="Transparent"
                         BorderThickness="0"
                         Padding="0"
                         VirtualizingStackPanel.IsVirtualizing="False"
                         ScrollViewer.CanContentScroll="False"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <ListBox.Resources>
                        <Style TargetType="ScrollBar">
                            <Setter Property="Width" Value="6"/>
                            <Setter Property="MinWidth" Value="6"/>
                            <Setter Property="MaxWidth" Value="6"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Margin" Value="2,0,0,0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ScrollBar">
                                        <Track x:Name="PART_Track" IsDirectionReversed="True">
                                            <Track.DecreaseRepeatButton>
                                                <RepeatButton Command="ScrollBar.PageUpCommand" Opacity="0" IsTabStop="False"/>
                                            </Track.DecreaseRepeatButton>
                                            <Track.Thumb>
                                                <Thumb>
                                                    <Thumb.Template>
                                                        <ControlTemplate TargetType="Thumb">
                                                            <Border x:Name="ThumbBorder"
                                                                    Background="{DynamicResource TextSecondaryBrush}"
                                                                    CornerRadius="3"
                                                                    Opacity="0.5"/>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter TargetName="ThumbBorder" Property="Opacity" Value="0.75"/>
                                                                </Trigger>
                                                                <Trigger Property="IsDragging" Value="True">
                                                                    <Setter TargetName="ThumbBorder" Property="Opacity" Value="0.9"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Thumb.Template>
                                                </Thumb>
                                            </Track.Thumb>
                                            <Track.IncreaseRepeatButton>
                                                <RepeatButton Command="ScrollBar.PageDownCommand" Opacity="0" IsTabStop="False"/>
                                            </Track.IncreaseRepeatButton>
                                        </Track>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.Resources>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <ContentPresenter/>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Image Grid.Column="0" Source="{Binding Icon}" Width="16" Height="16" Margin="0,0,6,0" VerticalAlignment="Center"
                                       Visibility="{Binding HasIcon, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                <TextBlock Grid.Column="1" Text="{Binding Name}" FontSize="11" Foreground="{DynamicResource TextPrimaryBrush}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                                <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="8,0,0,0" VerticalAlignment="Center">
                                    <TextBlock Text="&#xE92E;" FontFamily="Segoe MDL2 Assets" FontSize="9" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="0,0,2,0"/>
                                    <TextBlock Text="{Binding KeyPresses}" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                                </StackPanel>
                                <StackPanel Grid.Column="3" Orientation="Horizontal" Margin="8,0,6,0" VerticalAlignment="Center">
                                    <TextBlock Text="&#xE8B0;" FontFamily="Segoe MDL2 Assets" FontSize="9" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="0,0,2,0"/>
                                    <TextBlock Text="{Binding Clicks}" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>

            <!-- History Header + Chart Style -->
            <Grid Margin="4,6,0,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{x:Static p:Strings.Stats_HistoryHeader}" Style="{DynamicResource SectionTitle}" Margin="0" VerticalAlignment="Center"/>
                <Border Grid.Column="2" Background="{DynamicResource SubtleFillBrush}" CornerRadius="4" Padding="2">
                    <StackPanel Orientation="Horizontal">
                        <RadioButton GroupName="ChartStyle"
                                     Style="{DynamicResource SegmentedButton}" FontSize="10" Padding="6,3"
                                     IsChecked="{Binding SelectedChartStyleIndex, Converter={StaticResource IntToBoolConverter}, ConverterParameter=0}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE9D9;" FontFamily="Segoe MDL2 Assets" FontSize="9" VerticalAlignment="Center"/>
                                <TextBlock Text="{x:Static p:Strings.Chart_Line}" FontSize="10" VerticalAlignment="Center" Margin="2,0,0,0"/>
                            </StackPanel>
                        </RadioButton>
                        <RadioButton GroupName="ChartStyle"
                                     Style="{DynamicResource SegmentedButton}" FontSize="10" Padding="6,3"
                                     IsChecked="{Binding SelectedChartStyleIndex, Converter={StaticResource IntToBoolConverter}, ConverterParameter=1}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE9D2;" FontFamily="Segoe MDL2 Assets" FontSize="9" VerticalAlignment="Center"/>
                                <TextBlock Text="{x:Static p:Strings.Chart_Bar}" FontSize="10" VerticalAlignment="Center" Margin="2,0,0,0"/>
                            </StackPanel>
                        </RadioButton>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Range + Metric Selection Row -->
            <Grid Margin="0,0,0,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="6"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Range -->
                <Border Grid.Column="0" Background="{DynamicResource SubtleFillBrush}" CornerRadius="5" Padding="3">
                    <StackPanel Orientation="Horizontal">
                        <RadioButton Content="{x:Static p:Strings.Range_3Days}" GroupName="Range"
                                     Style="{DynamicResource SegmentedButton}" FontSize="11" Padding="6,4"
                                     IsChecked="{Binding SelectedRangeIndex, Converter={StaticResource IntToBoolConverter}, ConverterParameter=0}"/>
                        <RadioButton Content="{x:Static p:Strings.Range_7Days}" GroupName="Range"
                                     Style="{DynamicResource SegmentedButton}" FontSize="11" Padding="6,4"
                                     IsChecked="{Binding SelectedRangeIndex, Converter={StaticResource IntToBoolConverter}, ConverterParameter=1}"/>
                        <RadioButton Content="{x:Static p:Strings.Range_30Days}" GroupName="Range"
                                     Style="{DynamicResource SegmentedButton}" FontSize="11" Padding="6,4"
                                     IsChecked="{Binding SelectedRangeIndex, Converter={StaticResource IntToBoolConverter}, ConverterParameter=2}"/>
                        <RadioButton Content="{x:Static p:Strings.History_Range_All}" GroupName="Range"
                                     Style="{DynamicResource SegmentedButton}" FontSize="11" Padding="6,4"
                                     IsChecked="{Binding SelectedRangeIndex, Converter={StaticResource IntToBoolConverter}, ConverterParameter=3}"/>
                    </StackPanel>
                </Border>

                <!-- Metrics -->
                <Border Grid.Column="2" Background="{DynamicResource SubtleFillBrush}" CornerRadius="5" Padding="3">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <RadioButton Grid.Column="0" Content="{x:Static p:Strings.Metric_Clicks}" GroupName="Metric"
                                     Style="{DynamicResource SegmentedButton}" FontSize="11" Padding="6,4"
                                     IsChecked="{Binding SelectedMetricIndex, Converter={StaticResource IntToBoolConverter}, ConverterParameter=0}"/>
                        <RadioButton Grid.Column="1" Content="{x:Static p:Strings.Metric_Keys}" GroupName="Metric"
                                     Style="{DynamicResource SegmentedButton}" FontSize="11" Padding="6,4"
                                     IsChecked="{Binding SelectedMetricIndex, Converter={StaticResource IntToBoolConverter}, ConverterParameter=1}"/>
                        <RadioButton Grid.Column="2" Content="{x:Static p:Strings.Metric_Move}" GroupName="Metric"
                                     Style="{DynamicResource SegmentedButton}" FontSize="11" Padding="6,4"
                                     IsChecked="{Binding SelectedMetricIndex, Converter={StaticResource IntToBoolConverter}, ConverterParameter=2}"/>
                        <RadioButton Grid.Column="3" Content="{x:Static p:Strings.Metric_Scroll}" GroupName="Metric"
                                     Style="{DynamicResource SegmentedButton}" FontSize="11" Padding="6,4"
                                     IsChecked="{Binding SelectedMetricIndex, Converter={StaticResource IntToBoolConverter}, ConverterParameter=3}"/>
                    </Grid>
                </Border>
            </Grid>

            <!-- Chart -->
            <Border Style="{DynamicResource CardBorder}" Padding="6" Margin="0,0,0,4">
                <controls:StatsChartControl
                    ChartData="{Binding ChartData}"
                    LocalChartData="{Binding LocalChartData}"
                    ChartStyle="{Binding SelectedChartStyleIndex}"
                    SelectedMetricIndex="{Binding SelectedMetricIndex}"
                    Height="120"/>
            </Border>

            <!-- Mouse totals -->
            <TextBlock Text="{x:Static p:Strings.History_BreakdownHeader}" Style="{DynamicResource SectionTitle}"
                       Margin="4,2,0,6"/>
            <Border Style="{DynamicResource CardBorder}" Margin="0,0,0,4" Padding="10,9">
                <StackPanel>
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0"
                                Background="{DynamicResource SubtleFillBrush}"
                                CornerRadius="5"
                                Width="24"
                                Height="24"
                                Margin="0,0,8,0"
                                VerticalAlignment="Center">
                            <TextBlock Text="&#xE8B0;"
                                       FontFamily="Segoe MDL2 Assets"
                                       FontSize="12"
                                       Foreground="{DynamicResource AccentBrush}"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Grid.Column="1"
                                   Text="{x:Static p:Strings.Stats_MouseClickDetail}"
                                   FontSize="10"
                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                   VerticalAlignment="Center"/>
                    </Grid>

                    <Grid Margin="0,0,0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="6"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="6"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Background="{DynamicResource SubtleFillBrush}" CornerRadius="7" Padding="10,8">
                            <StackPanel>
                                <TextBlock Text="{x:Static p:Strings.Click_Left}" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                <TextBlock Text="{Binding HistoryLeftClicks}" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="2" Background="{DynamicResource SubtleFillBrush}" CornerRadius="7" Padding="10,8">
                            <StackPanel>
                                <TextBlock Text="{x:Static p:Strings.Click_Middle}" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                <TextBlock Text="{Binding HistoryMiddleClicks}" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="4" Background="{DynamicResource SubtleFillBrush}" CornerRadius="7" Padding="10,8">
                            <StackPanel>
                                <TextBlock Text="{x:Static p:Strings.Click_Right}" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                <TextBlock Text="{Binding HistoryRightClicks}" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="6"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Background="{DynamicResource SubtleFillBrush}" CornerRadius="7" Padding="10,8">
                            <StackPanel>
                                <TextBlock Text="{x:Static p:Strings.Click_Back}" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                <TextBlock Text="{Binding HistorySideBackClicks}" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="2" Background="{DynamicResource SubtleFillBrush}" CornerRadius="7" Padding="10,8">
                            <StackPanel>
                                <TextBlock Text="{x:Static p:Strings.Click_Forward}" FontSize="10" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                <TextBlock Text="{Binding HistorySideForwardClicks}" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,2,0,0"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </StackPanel>
            </Border>

            <Grid Margin="0,0,0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="4"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Style="{DynamicResource CardBorder}" Padding="9,8">
                    <controls:StatItemControl Icon="&#xE8AB;" Title="{x:Static p:Strings.Stats_MouseDistance}" Value="{Binding HistoryMouseDistance}"/>
                </Border>
                <Border Grid.Column="2" Style="{DynamicResource CardBorder}" Padding="9,8">
                    <controls:StatItemControl Icon="&#xE74A;" Title="{x:Static p:Strings.Stats_ScrollDistance}" Value="{Binding HistoryScrollDistance}"/>
                </Border>
            </Grid>

            <!-- Summary -->
            <TextBlock Text="{Binding HistorySummary}"
                       Style="{DynamicResource SecondaryText}" Margin="4,2,0,0"/>
            </StackPanel>
        </ScrollViewer>
    </Border>
</Window>
